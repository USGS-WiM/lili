export interface IReverseTranscription {
    id: number,
    reverse_transcription_no: number,
    vol_in: number,
    vol_out: number,
    rt_cq: number,
    insert_date: string,
    insert_user: string,
    update_date: string,
    update_user: string
}
