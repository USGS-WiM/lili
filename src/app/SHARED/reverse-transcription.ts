export interface IReverseTranscription {
    id: number,
    rt_no: number,
    extraction_id: number,
    vol_in: number,
    vol_out: number,
    rt_cq: number,
    rt_date: string,
    insert_date: string,
    insert_user: string,
    update_date: string,
    update_user: string
}
